"use client";

import MarketingCityCycleSubtitle from "./MarketingCityCycleSubtitle";

/** Client boundary so the subtitle interval mounts on the marketing shell. */
export default function MarketingCityCycleSubtitleLoader() {
  return <MarketingCityCycleSubtitle />;
}

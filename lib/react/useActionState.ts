"use client";

/**
 * Next.js 15 / React 19 `useActionState` equivalent on React 18.2: `useFormState` from `react-dom`
 * shares the same (state, formAction) server-action form contract.
 */
export { useFormState as useActionState } from "react-dom";

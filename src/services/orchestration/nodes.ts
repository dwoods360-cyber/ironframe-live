/**
 * Legacy sprint-10-3 linear graph nodes — quarantined (conflicts with `SovereignGraphState`).
 * Use `compileSovereignOrchestrationBus()` in `./graph.ts` for production ingest orchestration.
 */

export const ORCHESTRATION_NODES_DISABLED = true as const;

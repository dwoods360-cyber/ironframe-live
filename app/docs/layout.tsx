/**
 * Minimal docs segment layout — full docs chrome lives in [[...slug]]/page.tsx.
 * Keeps the route group isolated from Command Center shell assumptions.
 */
export default function DocsLayout({ children }: { children: React.ReactNode }) {
  return children;
}

/** Exact GRC string: must match purge ledger + `transitionThreatStatus` admin bypass. */
export const MANUAL_BOARD_PURGE_FOR_TEST_BASELINE_REASON =
  "Manual Board Purge for Test Baseline." as const;

export const GRC_RESOLUTION_GATE_ADMIN_BYPASS_DETAIL =
  "Administrative override for simulation reset" as const;

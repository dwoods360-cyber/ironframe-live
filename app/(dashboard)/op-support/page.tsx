import OpSupportWorkspace from "../opsupport/OpSupportWorkspace";

/** Alias route: `/op-support` — same workspace as `/opsupport` (Workforce Command Post at `#workforce`). */
export default function OpSupportAliasPage() {
  return <OpSupportWorkspace />;
}

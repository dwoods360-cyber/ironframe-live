node_modules
.next
out
build
.env*.local
.vercel
*.log
npm-debug.log*
logs/
!logs/.gitkeep
.DS_Store
.eslintcache
tmp-phase*.js
.tmp*.js

/lib/generated/prisma

# stryker temp files
.stryker-tmp

# Playwright local reports (never commit)
playwright-report/
test-results/

# Ironframe Local Runtime Noise
.pki-provision/
.env.production.local
.env.production.vercel
storage/constitutional/**/*.json
tsconfig.tsbuildinfo
.env
.env.*
!.env.example

# Accidental Explorer/Finder " copy" duplicates (never re-commit)
* copy
* copy.*
*copy.*
!lib/gtm/outreachReplyCopy.ts
!lib/gtm/outreachReplyReceiptCopy.ts
!tests/unit/outreachReplyCopy.test.ts
!tests/unit/outreachReplyReceiptCopy.test.ts
public copy/
test_docs copy/

# Loose development files and IDE noise
.cursor/
.vscode/
debug.zip
scripts/vercel-env-preview-pki.mjs
scripts/dev/stripe-catalog-artifact.json
# Live Path B / year-1 balance ID maps are tracked (ops continuity across workstations).
scripts/dev/stripe-client-links.json
secrets-replacements.txt
.env*

# Prospect enrichment scratch artifacts — contain contact PII (email/phone/LinkedIn).
# Repo is public; scratch data dumps must never be committable. Scratch *scripts*
# (scripts/dev/_*.mjs|.ts|.tsx) stay trackable — only their data outputs are ignored.
.prospeo-*.json
scripts/dev/_*.json
scripts/dev/_*.txt

# Local scratch + counsel binary exports (keep on disk; never commit)
/tmp/
docs/sales/exports/

# Google Cloud Platform IAM Keys
ironframe-prod-907f1402c60c.json

# Governance Frame Google Docs utility (OAuth + sync state)
scripts/governance-frame/google-docs/.state/
# Counsel packet Google Docs ID map is tracked; ignore other ops state noise
scripts/ops/.state/*
!scripts/ops/.state/counsel-packet-google-docs.json
**/google-oauth-client*.json
**/google-oauth-token*.json
secrets/google-oauth*.json
secrets/teams-graph*.json

# Ironleads local SQLite scratchpad (vitest + dev harvest)
Ironleads/data/

# Product-knowledge operator latch (Ops Hub floating notice)
lib/ironframeProductKnowledge/.drift-notice.json

# Local dump / bytecode noise (never commit)
daily_code_diff.txt
**/__pycache__/
*.py[cod]
*$py.class

# Local AI video production assets (refs/raw/selects)
Videos/


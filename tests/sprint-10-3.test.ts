import { describe, it } from "vitest";

/**
 * Legacy sprint-10-3 graph + AgentGraphState checkpoints — quarantined on main.
 * Covered by: tests/unit/sovereignOrchestrationBus.test.ts, tests/unit/ingestBusBridge.test.ts
 */
describe.skip("Sprint 10.3 linear workforce coordination (quarantined)", () => {
  it("placeholder", () => {
    // disabled — see sovereign bus tests
  });
});

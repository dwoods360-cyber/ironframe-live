/** GRC cold-store root (SSD mock) — shared copy for client + server UI. */
export const LKG_COLD_STORE_ROOT = "G:\\ironframe_store";

export const LKG_MANIFEST_PATH = `${LKG_COLD_STORE_ROOT}\\manifest\\lkg_signatures.json`;

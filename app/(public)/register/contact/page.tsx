import SalesContactClient from "@/app/components/marketing/SalesContactClient";

export const metadata = {
  title: "Contact sales · Ironframe",
  description: "Invite-only enterprise GRC onboarding — sales-assisted workspace provisioning.",
};

export default function SalesContactPage() {
  return <SalesContactClient />;
}
